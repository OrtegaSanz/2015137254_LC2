@model IEnumerable<_2015137254_ENT.Entities.TipoEvaluacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoEvaluacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoEvaluacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoEvaluacionId })
        </td>
    </tr>
}

</table>
